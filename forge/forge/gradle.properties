org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
# Do NOT enable org.gradle.configureondemand=true here — ForgeGradle 6 needs
# the project fully configured before it registers the runClient task.
