org.gradle.jvmargs=-Xmx4G
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
# Do NOT enable org.gradle.configureondemand=true — NeoGradle 7 also needs
# full project configuration before it registers run* tasks.
